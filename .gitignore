node_modules/
.next/
.vercel/
build/
dist/
coverage/
/public/synapse-logo.jpg
/public/synapse-logo.png
/public/team/
.DS_Store
*.log
*.tsbuildinfo
.env*
!.env.example

.supabase/
supabase/.temp/

.vercel
.turbo/

# Local editor workspace settings — never shared via the repo
.vscode/

# Generated readiness/reconciliation evidence — regenerated per run, and the
# reconciliation evidence file itself may contain operator-identifying data.
/artifacts/*
!/artifacts/readiness/
/artifacts/readiness/*
!/artifacts/readiness/PRODUCTION_GREEN_REPORT.json
!/artifacts/readiness/web-route-manifest.json
!/artifacts/readiness/route-integrity.json
!/artifacts/readiness/db-acl-inventory.json
!/artifacts/readiness/PRODUCTION_DB_RECONCILIATION.json
!/artifacts/readiness/CORE_OS_GREEN.json

# Temporary debug/probe scripts — may contain hardcoded production data
scripts/_tmp_*
.worktrees/

# Claude Code local config
.claude/

# Foglamp scan lock (editToken is a secret)
.foglamp/

