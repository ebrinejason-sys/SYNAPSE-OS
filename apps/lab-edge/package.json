{
  "name": "@synapse/lab-edge",
  "version": "0.1.0",
  "private": true,
  "description": "SYNAPSE Lab Edge — LAN analyzer gateway with durable offline queue",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "start": "node --import tsx src/cli.ts",
    "test": "tsx --test src/*.test.ts"
  },
  "engines": {
    "node": "22.x"
  },
  "devDependencies": {
    "tsx": "^4.21.0"
  }
}
