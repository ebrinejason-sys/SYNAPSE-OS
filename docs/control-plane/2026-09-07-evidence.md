# Control-plane evidence — 2026-09-07

Baseline: origin/main cb6c39a7737f5af9455172800fe5e5c285f023c6, refreshed from GitHub.
Work uses an isolated checkout. The Desktop checkout's three local commits and uncommitted Clinical/Lab changes were not modified.

## Verified database corrections

- `pharmacy_inventory_summary` now has `security_invoker=true`. A rolled-back transaction asserted that an authenticated role without tenant context cannot read inventory.
- Laboratory capability migration was tested in a rolled-back transaction and applied. Production queries confirm Lab Admin order-read/staff-write and scientist result-verification are enabled only for laboratory facilities. Technician verification and Lab Admin hospital staff-write are false.
- Migration filenames match the versions recorded by production Supabase.
- All public tables had RLS enabled at baseline. After the view correction, Supabase reports no ERROR-level advisories. Other Supabase advisory warnings still require review; this is not a blanket security certification.

## Application changes

- Resolve tenant context from the host, strip incoming tenant headers, reject session/host/path mismatches, and fail closed for missing, inactive, failed, or unavailable tenants.
- Require all mandatory provisioning steps to complete before activation. Persist failed Pharmacy invitation steps and preserve adapter/module metadata on retries.
- Preserve distinct synthetic facilities and Lab tenant linkage; assign Lab Admin rather than Hospital Admin; limit default Lab modules and departments.
- Support laboratory staff contexts only on Lab APIs and scoped staff administration. Route Lab accounts to the Lab workspace.
- Replace emailed staff temporary passwords with secure invitation links and restrict assignable staff roles.
- Add a synthetic acceptance selector. Test Center probes wildcard HTTPS and unknown tenant handling live and leaves missing acceptance evidence blocked.

## Verification

- Control-plane regression tests: 39 passed (24 routing, 7 staff-role/Lab landing checks, 8 provisioning orchestration using an in-memory PostgREST adapter). These are not live database acceptance tests.
- Pharmacy suite: 218 passed, 5 skipped; separate FEFO suite: 3 passed.
- Department uniqueness, Lab result persistence, and hospital catalog: 10 passed.
- Migration folder integrity: 68 files passed.
- Production builds and typechecks are required before commit; build logs are retained in the task workspace.

## Remaining acceptance

Real deployed UI acceptance, administrator invitation redemption/logins, standalone Pharmacy POS/inventory/sale/receipt/isolation, Lab staff/workspace, domain-test and negative wildcard cases, retry evidence, and Test Center completion remain outstanding. SKIPPED and BLOCKED are not PASS. No Lab expansion is authorized by this evidence.
